pub def main()
    val x = 'a'
    @print("{c}\n", {x})
    match x
        'a' => (
            @puts("great")
        )
        'b' => (
            @puts("uh...")
        )
        _ => (
            @puts("what.")
        )
    end
end

