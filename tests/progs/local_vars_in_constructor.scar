import "std/random"

class SomeCustomType:
    init:
        string some_string = "hello"
        print "%s" | some_string
        i32 x = random::num(0, 100)
        print "%d" | x
        i32 this.y = x * 2
        print "%d" | this.y

var sct = new SomeCustomType()
