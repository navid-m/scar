import "std/collections"

## Execute a command.
pub fn exec(string command) -> i64:
    $raw (
        return system(command);
    )

## Execute a command with arguments.
pub fn exec_with_args(string command, collections::StringArrayList args) -> i64:
    string cmdline = command
    int n = collections::StringArrayList::get_size(args)
    int i = 0
    while i < n:
        cmdline = fmt!("%s %s", cmdline, collections::StringArrayList::get_element(args, i))
        i = i + 1
    return system(cmdline)

## Exit the program.
pub fn exit(i64 code):
    $raw (
        exit(code);
    )

## Get the current working directory.
pub fn cwd() -> char*:
    $raw (
        char *buffer = getcwd(NULL, 0);
        char *result = buffer;
        return result;
    )

## Change the current working directory.
pub fn chdir(string path) -> int:
    $raw (
        return chdir(path);
    )

## Get the home directory.
pub fn homedir() -> char*:
    $raw (
        char *home = getenv("HOME");
        if (home == NULL) {
            home = getenv("USERPROFILE");
        }
        return home ? strdup(home) : NULL;
    )

## Get an environment variable.
pub fn get_env(string name) -> char*:
    $raw (
        char *value = getenv(name);
        return value ? strdup(value) : NULL;
    )

## Get the temporary directory.
pub fn tempdir() -> char*:
    $raw (
        char *temp = getenv("TMPDIR");
        if (temp == NULL) {
            temp = getenv("TEMP");
        }
        if (temp == NULL) {
            temp = getenv("TMP");
        }
        return temp ? strdup(temp) : NULL;
    )

## Get the command line arguments.
pub fn get_args() -> collections::StringArrayList:
    var args = new collections::StringArrayList(4)
    for i = 0 to __global_argc - 1:
        args.add_element(__global_argv[i])
    return args

## Clear the terminal.
pub fn clear():
    external import "stdio.h"

    platform (Windows):
        system("cls")
    platform (Posix):
        system("clear")
