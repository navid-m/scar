pub fn length(string str) -> int:
    $raw (
        return strlen(str);
    )

pub fn compare(string a, string b) -> int:
    $raw (
        return strcmp(a, b);
    )

pub fn ord(string str) -> int:
    $raw (
        if (str == NULL || str[0] == '\0') {
            return 0;
        }
        return (int)str[0];
    )

pub fn ord_char(char c) -> int:
    return (int)c;

pub fn chr(int ascii_value) -> char*:
    $raw (
        char result[2];
        if (ascii_value < 0 || ascii_value > 255) {
            result[0] = '\0';
        } else {
            result[0] = (char)ascii_value;
            result[1] = '\0';
        }
        return strdup(result);
    )

pub fn trim_space(string str) -> string:
    $raw (
        if (str == NULL) {
            strcpy(_output_buffer, "");
            return;
        }
        char *start = str;
        char *end = str + strlen(str) - 1;
        while (*start && (*start == ' ' || *start == '\t' || *start == '\n' || *start == '\r')) {
            start++;
        }
        while (end > start && (*end == ' ' || *end == '\t' || *end == '\n' || *end == '\r')) {
            end--;
        }
        size_t len = end - start + 1;
        strncpy(_output_buffer, start, len);
        _output_buffer[len] = '\0';
    )

pub fn trim_prefix(string str, string prefix) -> string:
    $raw (
        if (str == NULL || prefix == NULL) {
            strcpy(_output_buffer, str ? str : "");
            return;
        }
        size_t prefix_len = strlen(prefix);
        size_t str_len = strlen(str);
        if (str_len >= prefix_len && strncmp(str, prefix, prefix_len) == 0) {
            strcpy(_output_buffer, str + prefix_len);
        } else {
            strcpy(_output_buffer, str);
        }
    )

pub fn trim_suffix(string str, string suffix) -> char*:
    $raw (
        if (str == NULL || suffix == NULL) {
            return str ? str : "";
        }
        size_t suffix_len = strlen(suffix);
        size_t str_len = strlen(str);
        if (str_len >= suffix_len && strcmp(str + str_len - suffix_len, suffix) == 0) {
            size_t result_len = str_len - suffix_len;
            char buffer[256];
            strncpy(buffer, str, result_len);
            buffer[result_len] = '\0';
            return buffer;
        } else {
            return str;
        }
    )

pub fn ends_with(string str, string suffix) -> bool:
    $raw (
        if (str == NULL || suffix == NULL) {
            return false;
        }
        
        size_t str_len = strlen(str);
        size_t suffix_len = strlen(suffix);
        
        if (suffix_len > str_len) {
            return false;
        }
        
        return strcmp(str + str_len - suffix_len, suffix) == 0;
    )

pub fn contains(string str, string substring) -> bool:
    $raw (
        if (str == NULL || substring == NULL) {
            return false;
        }
        return strstr(str, substring) != NULL;
    )

pub fn to_hex(int value) -> char*:
    $raw (
        char *result = malloc(16);
        if (result == NULL) {
            return strdup("");
        }
        
        if (value < 0) {
            strcpy(result, "0");
            return result;
        }
        
        sprintf(result, "%X", value);
        return result;
    )

pub fn equal(string a, string b) -> bool:
    $raw (
        if (a == NULL && b == NULL) {
            return true;
        }
        if (a == NULL || b == NULL) {
            return false;
        }
        
        return strcmp(a, b) == 0;
    )

pub fn replace(string str, string old_substr, string new_substr) -> char*:
    $raw (
        if (str == NULL || old_substr == NULL || new_substr == NULL) {
            return str ? strdup(str) : strdup("");
        }
        
        int old_len = strlen(old_substr);
        int new_len = strlen(new_substr);
        int str_len = strlen(str);
        
        if (old_len == 0) {
            return strdup(str);
        }
        int count = 0;
        char *temp = str;
        while ((temp = strstr(temp, old_substr)) != NULL) {
            count++;
            temp += old_len;
        }
        
        if (count == 0) {
            return strdup(str);
        }
        
        int result_len = str_len + count * (new_len - old_len);
        char *result = malloc(result_len + 1);
        if (result == NULL) {
            return strdup("");
        }
        
        char *src = str;
        char *dst = result;
        
        while ((temp = strstr(src, old_substr)) != NULL) {
            int prefix_len = temp - src;
            strncpy(dst, src, prefix_len);
            dst += prefix_len;
            strcpy(dst, new_substr);
            dst += new_len;
            src = temp + old_len;
        }        
        strcpy(dst, src);
        return result;
    )

pub fn split(string str, string delimiter) -> collections::StringArrayList:
    var result = new collections::StringArrayList(10)
    if strings::length(str) == 0:
        result.add_element("")
        return result
    
    if strings::length(delimiter) == 0:
        result.add_element(str)
        return result
    string unique_marker = "\x01"
    string marked_str = strings::replace(str, delimiter, unique_marker)
    int marker_ascii = ord(unique_marker)
    string current_part = ""
    int str_len = strings::length(marked_str)
    int i = 0
    while i < str_len:
        string current_char = strings::get_char_at(marked_str, i)
        if ord(current_char) == marker_ascii:
            result.add_element(current_part)
            current_part = ""
        else:
            current_part = fmt!("%s%s", current_part, current_char)
        i = i + 1
    result.add_element(current_part)
    return result

pub fn get_char_at(string str, int index) -> string:
    $raw (
        if (str == NULL || index < 0 || index >= strlen(str)) {
            return "";
        }
        char result[2];
        result[0] = str[index];
        result[1] = '\0';
    )
    return result
