pub fn readln() -> string:
    $raw (
        char buffer[1024];
        if (fgets(buffer, sizeof(buffer), stdin) != NULL) {
            size_t len = strlen(buffer);
            if (len > 0 && buffer[len - 1] == '\n') {
                buffer[len - 1] = '\0';
            }
            return buffer;
        } else {
            return "";
        }
    )

pub fn readln_with_prompt(string prompt) -> string:
    print "%s" | prompt
    return io_readln()
