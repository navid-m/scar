pub macro NewArray(class_name, field_type, elem_type, cType, d_return, alloc_expr, free_expr, copy_expr, get_check):
    pub class class_name:
        init(i32 initial_capacity):
            this.size = 0
            this.capacity = initial_capacity
            ref field_type this.data = nil
            $raw (
                this->data = malloc(this->capacity * sizeof(cType));
            )

        fn add_element(elem_type value) -> void:
            if this.size >= this.capacity:
                this.resize()
            $raw (
                ((cType*)this->data)[this->size] = copy_expr;
            )
            this.size = this.size + 1

        fn get_element(i32 index) -> elem_type:
            if index < 0 || index >= this.size:
                return d_return
            $raw (
                get_check;
            )

        fn set_element(i32 index, elem_type value) -> void:
            if index >= 0 && index < this.size:
                $raw (
                    free_expr
                    ((cType*)this->data)[index] = copy_expr;
                )

        fn get_size() -> i32:
            return this.size

        fn resize() -> void:
            this.capacity = this.capacity * 2
            $raw (
                this->data = realloc(this->data, this->capacity * sizeof(cType));
            )

NewArray(ArrayList, i32, i32, int, -1,malloc(this->capacity * sizeof(int)), /* free_expr */, value,  return ((i32*)this->data)[index];)
NewArray(FloatArrayList, f32, f32, float, 0.0, malloc(this->capacity * sizeof(float)), /* free_expr */, value, return ((f32*)this->data)[index];)
NewArray(CharArrayList, char, char, char, '\0', malloc(this->capacity * sizeof(char)),  /* free_expr */, value,return ((char*)this->data)[index];)
NewArray(BooleanArrayList, bool, bool, bool, false, malloc(this->capacity * sizeof(bool)), /* free_expr */, value, return ((bool*)this->data)[index];)
NewArray(StringArrayList, char, string, char*, "",malloc(this->capacity * sizeof(char*)),free(((char**)this->data)[index]);,strdup(value),char* value = ((char**)this->data)[index];if (value == nil) {return ""; } return value;)
